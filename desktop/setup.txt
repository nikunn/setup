ERROR: Media change: please insert the disc labeled
	vi /etc/apt/sources.list
	comment the cdrom lines

ERROR: is not in the sudoers file
	adduser _USER_ sudo

Virtual box addition
	apt-get install build-essential module-assistant
	m-a prepare
	cd /media && mkdir vbox
	sudo mount /dev/sr0 vbox
	sudo ./VBoxLinuxAdditions.run

INSTALL 
- vim
- git
- lua
- premake
- avrdude avrdude-doc binutils-avr avr-libc gcc-avr gdb-avr


REMOVE
sudo apt-get remove --purge libreoffice*
sudo apt-get clean
sudo apt-get autoremove

sudo apt-get remove tracker

VIRTUAL BOX
Mount VB Shared Folder
  sudo mount -t vboxsf Shared ~/Shared/
Enable symlink for VB shared folder
  VBoxManage setextradata VM_NAME VBoxInternal2/SharedFoldersEnableSymlinksCreate/SHARE_NAME 1
